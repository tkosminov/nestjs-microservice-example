{
  "name": "@micro/service-customer",
  "version": "1.0.0",
  "description": "",
  "author": "t.kosminov",
  "private": true,
  "scripts": {
    "test": "jest --config ../../jest.config.ts",
    "build": "rimraf dist && tsc -p ./tsconfig.build.json",
    "start": "ts-node --files -r tsconfig-paths/register ./src/main.ts",
    "start:build": "node -r ts-node/register -r tsconfig-paths/register ./dist/service-customer/src/main.js",
    "start:dev": "nodemon"
  }
}
